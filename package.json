{
  "name": "callummr.github.io",
  "description": "Personal site",
  "version": "1.0.0",
  "author": "Callum Mellor-Reed <me@callummr.com>",
  "dependencies": {
    "gatsby": "^1.9.45",
    "gatsby-link": "^1.6.17",
    "gatsby-plugin-favicon": "^1.0.0",
    "gatsby-plugin-google-analytics": "^1.0.8",
    "gatsby-plugin-offline": "^1.0.9",
    "gatsby-plugin-react-helmet": "^1.0.5",
    "gatsby-plugin-styled-components": "^1.0.5",
    "react-syntax-highlighter": "^5.7.0",
    "styled-components": "^2.2.1"
  },
  "license": "UNLICENSED",
  "main": "n/a",
  "scripts": {
    "build": "gatsby build",
    "develop": "gatsby develop",
    "format":
      "prettier --trailing-comma es5 --no-semi --single-quote --write 'src/**/*.js'",
    "predeploy": "npm run format && npm run build",
    "deploy": "git subtree push --prefix public origin master",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "prettier": "^1.6.1",
    "svg-inline-loader": "^0.8.0"
  }
}
